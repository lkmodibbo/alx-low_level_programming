#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
* main - Entry point
* Return: Always 0 (success)
*/
int main(void)
int d;
int p;
for (d = '0'; d < '9' d++)
{
for (p = d + 1; P <= '9' p++)
{
putchar(d);
putchar(p);
return (0);
}
